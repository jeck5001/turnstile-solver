# Standalone Cloudflare Turnstile Solver
# 默认 browser=camoufox，无需 patchright（Docker 镜像因此更小/更快）
# 若要用 chromium/chrome/msedge：pip install 'patchright>=1.50' && python -m patchright install chromium
# 不装 camoufox[geoip]（MaxMind mmdb 体积大，解题不需要）
# python-dotenv 未使用，不装
quart>=0.19
hypercorn>=0.14
camoufox>=0.4
rich>=13
requests>=2.31
